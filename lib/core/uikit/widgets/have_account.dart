

import 'package:flutter/material.dart';
/*


class HaveAnAccount extends StatelessWidget {
  const HaveAnAccount({
    super.key, required this.isLoginScreen, this.onTap,
  });
  final bool isLoginScreen;
  final Function()? onTap;
  @override
  Widget build(BuildContext context) {
    return Row(
      mainAxisAlignment: MainAxisAlignment.center,
      children: [
        Text(
          isLoginScreen ? 'حساب کاربری ندارید؟  ' : 'قبلا ثبت‌نام کرده‌اید ؟ ',
          style: loginOrangeStyle(),
        ),
        GestureDetector(
          onTap: onTap,
          child: Text(
            isLoginScreen ? 'ثبت نام' : 'ورود',
            style:
            loginOrangeStyle().apply(decoration: TextDecoration.underline),
          ),
        )
      ],
    );
  }
}
*/
